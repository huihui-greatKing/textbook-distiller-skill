interface:
  display_name: "Textbook Distiller"
  short_description: "OCR and distill textbooks into reusable AI skills"
  default_prompt: "Use $textbook-distiller to distill the provided textbook. If I explicitly authorize full-run mode, continue through OCR, chapter summaries, skills, tests, and report unless a serious blocker appears."
